<%
// Test page to verify plugin execution
const debug = {
  hasRequest: !!request,
  pluginRan: !!request._pluginRan,
  hasAuth: !!request.auth,
  requestKeys: Object.keys(request),
  authValue: request.auth
};

response.json(200, debug);
%>
